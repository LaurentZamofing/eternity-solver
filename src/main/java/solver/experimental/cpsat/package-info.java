/**
 * Phase-D experimental: CP-SAT (Google OR-Tools) spike.
 *
 * <p>Not implemented yet — see {@code README.md} for the pinned design
 * and the go/no-go criteria. Keeping the package empty in main so the
 * intent is discoverable without adding the OR-Tools native dependency
 * until a branch proves it's worth it.</p>
 */
package solver.experimental.cpsat;
